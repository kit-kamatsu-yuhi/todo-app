---
name: review
description: |
  コードレビューを実行する。現在の変更差分に対して、ルール準拠・セキュリティ・テストカバレッジ・パフォーマンスの観点でレビューを行う。
  TRIGGER when: 「レビューして」「コードチェックして」「差分見て」など、コードレビューを依頼されたとき。
  DO NOT TRIGGER when: PR のレビューコメントへの対応（→ address-pr-review）、セキュリティ監査（→ security-audit スキル）。
argument-hint: "[対象ファイルやPR番号（省略可）]"
---

# コードレビュー実行

$ARGUMENTS に対してコードレビューを実施する。

## レビュー手順

1. **変更差分の取得**
   - `git diff` または指定された PR の差分を取得する
   - 変更ファイル一覧と変更量を把握する

2. **ルール準拠チェック**
   - `naming` rule: 命名規則に従っているか
   - `code-quality` rule: 複雑度、型安全性
   - `error-handling` rule: エラーハンドリングが適切か
   - `commit-message` rule: コミットメッセージが規約に準拠しているか

3. **セキュリティチェック**
   - `security` skill の観点に従う
   - OWASP Top 10 の脆弱性がないか
   - シークレットがハードコードされていないか

4. **テストチェック**
   - `testing` skill の観点に従う
   - テストが追加・更新されているか
   - カバレッジ基準（80%以上）を満たしているか

5. **パフォーマンスチェック**
   - N+1 クエリがないか
   - 不要な再計算・ループがないか

6. **ドキュメント更新チェック**
   - `wiki/` / `raw/` ドキュメントの更新が含まれているか
   - 最低でも `raw/` に該当 Issue のコンテキストを追加しているか

## 出力フォーマット

レビュー結果を以下の重要度で分類して出力する:
- **Must**: 修正必須
- **Should**: 修正推奨
- **Nit**: 軽微な指摘
