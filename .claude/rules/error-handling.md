---
paths: "**/*.ts, **/*.tsx, **/*.py, **/*.go, **/*.kt, **/*.kts, **/*.rs, **/*.java, **/*.swift"
---

# エラーハンドリング

言語固有のルールは `languages/` 配下の各言語ファイルを参照。

## 共通方針

- リトライ戦略: 指数バックオフ（初回1秒、最大30秒、最大3回）
- 外部サービス呼び出しにはタイムアウトを設定する
- エラーログには十分なコンテキスト（リクエストID、ユーザーID等）を含める
