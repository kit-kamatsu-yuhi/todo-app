---
paths: "**/*.ts, **/*.tsx, **/*.py, **/*.go, **/*.kt, **/*.kts, **/*.rs, **/*.java, **/*.swift"
---

# 過去レビュー知見

ソースコードの実装・変更時に `wiki/pages/reviews/` の過去レビュー知見を参照し、同じ問題の再発を防ぐ。

新たなレビュー知見が得られた場合は `wiki/pages/reviews/` に保存・更新する（`/address-pr-review` スキルが担当）。
