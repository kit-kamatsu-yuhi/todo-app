---
paths: "**/*.ts, **/*.tsx, **/*.py, **/*.go, **/*.kt, **/*.kts, **/*.rs, **/*.java, **/*.swift"
---

# コード品質

言語固有のルールは `languages/` 配下の各言語ファイルを参照。

## 共通ルール

- デッドコード（使われていないコード）は削除する
- マジックナンバーは定数として定義する
- DRY 原則に従うが、過度な抽象化は避ける
- コメントは「なぜ」を説明する（「何を」はコードで表現する）
