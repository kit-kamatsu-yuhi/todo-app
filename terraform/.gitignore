# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# 保存済みプラン（機密を含み得る・使い捨て）
tfplan
tfplan*
*.tfplan
# 変数上書きファイル（機密が入り得るため）。terraform.tfvars は例として追跡する。
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
